
var Assert = org.junit.Assert;

java.lang.System.err.println( "tests 1: " + tests.length );
it( "should work", function() {
  //Assert.fail( "damnit" );
} );

java.lang.System.err.println( "tests 2: " + tests.length );

it( "should also work", function() {

} );

java.lang.System.err.println( "tests 3: " + tests.length );

